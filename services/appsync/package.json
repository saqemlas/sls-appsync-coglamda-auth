{
  "name": "@aws-template/appsync",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf node_modules .serverless",
    "ci": "yarn install --frozen-lockfile",
    "checktsc": "tsc --noEmit",
    "test": "yarn run jest --passWithNoTests",
    "deploy": "yarn run serverless deploy",
    "remove": "yarn run serverless remove"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "3.345.0",
    "@aws-sdk/types": "3.342.0"
  },
  "devDependencies": {
    "@aws-template/common": "1.0.0"
  }
}
